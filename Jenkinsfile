pipeline {
    agent any
    
    stages {
        stage('Greeting') {
            steps {
                echo "Hello, World!"
                echo "$GIT_BRANCH"
            }
        }
    }
}